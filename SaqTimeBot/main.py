# region IMPORTSimport configparser as cpimport openaifrom apscheduler.schedulers.background import BackgroundSchedulerfrom mtranslate import translatefrom pyrogram import Client, filtersfrom crud import *from keyboards import *from utils import *import osimport sys# endregionDEBUG = True# region CASHswitch = {    'admin': {},    'private_check_register': {},    'private_check_phone': {},    'private_change_data': {},    'private_correct_data': {},    'private_change_number': {},    'private_change_fio': {},    'private_change_birthdate': {},    'private_change_custnum': {},    'private_change_description': {},    'private_change_email': {},    'private_create_tt': {},    'private_create_firsttime': {},    'private_change_tt': {},    'private_create_jumatable': {},    'private_register_needy': {},    'private_get_feedback': {},    'private_get_idea': {},    'group_': {},    'supergroup_': {},}data = {}global_cash = {}# endregion# region DEFSswitch_starter = Truedef start_upd(app):    with app:        app.send_message(int(admin_chat[0]), 'Запуск успешно инициализирован!🍀',                         reply_to_message_id=int(admin_chat[1]))    global switch_starter    switch_starter = Falsedef restart_upd(app):    log("Перезагрузка бота...")    python = sys.executable    os.execl(python, python, *sys.argv)    with app:        app.send_message(int(admin_chat[0]), 'Рестарт бота!🍀',                         reply_to_message_id=int(admin_chat[1]))def switch_off(id):    global switch    for key, item in switch.items():        switch[key][id] = Falsedef switch_turn(id, k):    global switch    for key, item in switch.items():        switch[key][id] = False    switch[k][id] = Truedef getAnketa(datasss, request='null'):    if request == 'adm_request':        text = 'Запрос на регистрацию\n\n'        age = calculate_age(datasss['birthdate'])        text += f'<b>{datasss["fio"]}</b>\n'        text += f"Хэпик - <i>{datasss['birthdate']} ({age} лет)</i>\n"        text += f"номер - <i>{datasss['number']}</i>\n"        text += f"Почта - <i>{datasss['email']}</i>\n\n"        text += f"Родитель (опекун) - <i>{datasss['custody_number']}<i>\n\n"        text += f"Заметка - <i>{datasss['description']}</i>"    elif request == 'new_admin':        text = '<b>ЗАПРОС НА АДМИНА</b>\n\n'        age = calculate_age(datasss['birthdate'])        text += f'<b>{datasss["fio"]}</b>\n'        text += f"Хэпик - <i>{datasss['birthdate']} ({age} лет)</i>\n"        text += f"номер - <i>{datasss['number']}</i>\n"        text += f"Почта - <i>{datasss['email']}</i>\n\n"        text += f"Родитель (опекун) - <i>{datasss['custody_number']}<i>\n\n"        text += f"Заметка - <i>{datasss['description']}</i>"    else:        age = calculate_age(datasss['birthdate'])        text = f'<b>{datasss["fio"]}</b>\n'        text += f"Хэпик - <i>{datasss['birthdate']} ({age} лет)</i>\n"        text += f"номер - <i>{datasss['number']}</i>\n"        text += f"Почта - <i>{datasss['email']}</i>\n\n"        text += f"Родитель (опекун) - <i>{datasss['custody_number']}<i>\n\n"        text += f"Заметка - <i>{datasss['description']}</i>"    return textdef getAnketaInUM(user):    age = calculate_age(user['birthdate'])    text = f'<b>{user["fio"]}</b>\n'    text += f"Хэпик - <i>{user['birthdate']} ({age} лет)</i>\n"    text += f"номер - <i>{user['number']}</i>\n"    text += f"Почта - <i>{user['email']}</i>\n\n"    text += f"Родитель (опекун) - <i>{user['custody_number']}<i>\n\n"    text += f"Заметка - <i>{user['description']}</i>\n\n"    if user['verified']:        text += f'Верификация пройдена!'    else:        text += f'Не верифицирован(а)'    return textdef getVerifyAdmins(user):    text = getAnketa(user, 'adm_request')    user_id = user['id']    ikb_get_admin_request = InlineKeyboardMarkup(        inline_keyboard=[            [InlineKeyboardButton("Подтвердить", callback_data=f'admchat_request_succes_{user_id}'),             InlineKeyboardButton("Отклонить", callback_data=f'admchat_request_denied_{user_id}')]        ]    )    app.send_message(int(admin_chat[0]), text, reply_markup=ikb_get_admin_request,                     reply_to_message_id=int(admin_chat[1]))def getNewAdmin(id):    user = app.get_users(id)    last_name = user.last_name    if not last_name:        last_name = ''    text = f"Запрос на админку\n"    text += f'Юзер - {getUserLink(user.id, f"{user.first_name}{last_name}")}'    if user.username:        text += f"@{user.username}"    ikb_get_admin_request = InlineKeyboardMarkup(        inline_keyboard=[            [InlineKeyboardButton("Подтвердить", callback_data=f'admchat_newadm_succes_{id}'),             InlineKeyboardButton("Отклонить", callback_data=f'admchat_newadm_denied_{id}')]        ]    )    app.send_message(int(admin_chat[0]), text, reply_to_message_id=int(admin_chat[1]),                     reply_markup=ikb_get_admin_request)def getVMKB(upt):    if upt:        this = upt[0]        next = upt[1]        this = format_dates([this])[0]        next = format_dates([next])[0]    else:        this = '##:##'        next = '##:##'    kb_menu = ReplyKeyboardMarkup(        keyboard=[            [KeyboardButton(f"Записаться на эту неделю ({next})")],            [KeyboardButton(f"Записаться на следующую неделю ({this})")],            [KeyboardButton("Мое расписание")],            [KeyboardButton("Информация о текущих нуждающихся")],            [KeyboardButton("Профиль"), KeyboardButton("Предложка и обратная связь")]        ],        resize_keyboard=True    )    return kb_menudef getVMKBadmin():    kb_menu = ReplyKeyboardMarkup(        keyboard=[            [KeyboardButton("Админка")],            [KeyboardButton(f"Создать расписание"), KeyboardButton("Расписание")],            [KeyboardButton("Топ"), KeyboardButton("Статистика")],            [KeyboardButton("Информация о текущих нуждающихся")],            [KeyboardButton("Зарегистрировать нуждающуюся")],        ],        resize_keyboard=True    )    return kb_menudef SENDALL(text):    users = UM.get_all_users_data()    tt = UML.get_all_date_data()    upd = []    for time in tt:        t = next(iter(time))        date, _ = t.split('-')        upd.append(date)    upt = get_closest_dates(upd)    log(upt)    if users:        for user in users:            app.send_message(user['id'], text, reply_markup=getVMKB(upt))        return True    else:        return Falsedef console(text='debug'):    app.send_message(int(admin_chat[0]), text,                     reply_to_message_id=int(admin_chat[1]))    log(text.replace('\n', '  '), 'INFO')# endregion# region STARTcfg = cp.ConfigParser()cfg.read('config.ini')token_bot = cfg.get('API_KEYS', 'tokenBot')api_hash = cfg.get('API_KEYS', 'apiHash')api_id = int(cfg.get('API_KEYS', 'apiId'))openai_api = cfg.get('API_KEYS', 'openai_api')admin_chat = cfg.get('ADMIN_CHAT', 'chat').split('_')openai.api_key = openai_apiadmin_pack = cfg.items('ADMIN_LIST')admins = []for admin in admin_pack:    admins.append(int(admin[1]))log(admins)app = Client("my_bot", api_id=api_id, api_hash=api_hash, bot_token=token_bot)app_starter = Client("my_bot12345", api_id=api_id, api_hash=api_hash, bot_token=token_bot)# endregion# region NEW MEMBERS@app.on_message(filters.new_chat_members)def new_chat_members(client, message):    for member in message.new_chat_members:        if member.id == client.me.id:  # Проверка на идентификатор вашего бота            client.send_message(                message.chat.id,                "Привет, я новый помощник и ассистент волонтерского проэкта Сактайм!\nЯ помогу вам более удобнее управлять логистикой\nДля начала сактаймерам нужно зарегистрироваться, в лс, для дальнейшей работы",            )        elif member.username:            client.send_message(                message.chat.id,                f"Привет, {member.username}! Добро пожаловать в чат!",            )# endregion# region CONTACT_REGISTER@app.on_message(filters.contact & filters.private)def contact_register(_, msg):    if switch['private_check_phone'][msg.from_user.id]:        data[msg.from_user.id]['number'] = msg.contact.phone_number        msg.reply_text('Отлично! Вот ваша анкета:', reply_markup=kb_delete)        msg.reply_text(getAnketa(data[msg.from_user.id]))        msg.reply_text('Хотите еще что нибудь изменить?', reply_markup=kb_change_q)        switch['private_check_phone'][msg.from_user.id] = False        switch['private_change_data'][msg.from_user.id] = True        switch['private_check_register'][msg.from_user.id] = False    elif switch['private_change_data'][msg.from_user.id]:        data[msg.from_user.id]['number'] = msg.contact.phone_number        msg.reply_text('Номер был изменен')        msg.reply_text('Анкета:')        msg.reply_text(getAnketa(data[msg.from_user.id]))        msg.reply_text('Хотите еще что нибудь изменить?', reply_markup=kb_change_q)        switch['private_check_register'][msg.from_user.id] = False        switch['private_change_data'][msg.from_user.id] = True        switch['private_check_phone'][msg.from_user.id] = False# endregion# region GROUP@app.on_message(filters.text & filters.group)def group(_, msg):    execute = msg.text    cmd = Refactor(execute)    sliced = Refactor(execute, slice=True)    user = msg.from_user    # region SWITCHMODE    global switch    for key, value in switch.items():        try:            if switch[key][msg.from_user.id]:                pass        except KeyError:            switch[key] = {msg.from_user.id: False}    # endregion    chat_id = msg.chat.id    try:        theme = msg.reply_to_message.id    except:        theme = None    if user.id in admins:        if cmd == 'setup':            msg.reply_text('Чат установлен в качестве Админ чата')            update_config(cfg, 'ADMIN_CHAT', 'chat', f'{msg.chat.id}_{msg.reply_to_message.id}')        elif cmd == 'quit':            msg.reply_text('Спокойной ночи❄')            quit()        elif sliced[0].lower() == 'cакбот' and len(sliced) > 1:            log(sliced)            text = execute.replace('сакбот', '')            response = ChatGptSys(app, msg, openai, translate, text)            msg.reply_text(response)    elif theme != int(admin_chat[1]):        if sliced[0].lower() in ['start', 'start@saqbibot']:            text = 'Привет я ваш ассистент!\n'            text += 'Я помогу вам логистически управлять системой волонтерства SaqTime!\n'            text += 'Надеюсь мы все поладим\n'            text += 'А еще, я в основном работаю в лс, там все необходимое\n'            msg.reply_text(text)        elif sliced[0].lower() == 'cакбот' and len(sliced) > 1:            log(sliced)            text = execute.replace('сакбот', '')            response = ChatGptSys(app, msg, openai, translate, text)            msg.reply_text(response)    else:        app.delete_messages(chat_id, msg.id)        log(f'deleted {msg.text}')# endregion# region PRIVATE@app.on_message(filters.text & filters.private)def mainloop(client, msg):    execute = msg.text    cmd = Refactor(execute)    sliced = Refactor(execute, slice=True)    user = msg.from_user    # region SWITCHMODE    global switch    for key, value in switch.items():        try:            if switch[key][msg.from_user.id]:                pass        except KeyError:            switch[key] = {msg.from_user.id: False}    global global_cash    try:        if global_cash[msg.from_user.id]:            pass    except KeyError:        global_cash = {msg.from_user.id: {}}    # endregion    chat_id = msg.chat.id    chat_info = app.get_chat(chat_id)    chat_type = chat_info.type    check_in_db = UM.check_in_db(msg.from_user.id)    if check_in_db:        user_data = UM.get_user_data(msg.from_user.id)        if user_data:            verified = user_data[0]['verified']        else:            user_data = None            verified = False    else:        user_data = None        verified = False    # region ADMIN    if user.id in admins:        if cmd == 'debug':            msg.reply_text()            scheduler = BackgroundScheduler()            scheduler.add_job(send_message, 'cron', hour=12, args=[                app,                chat_id,                msg.text])  # Отправлять сообщение в 12:00 каждый день с пользовательским текстом            # Запуск планировщика            scheduler.start()        elif cmd == 'restart':            msg.reply_text('Перезагрузка!🖥️')            restart_upd(app_starter)        elif execute == 'Админка':            msg.reply_text('Coming soon🍀')        elif execute == 'Локации':            msg.reply_text('Coming soon🍀')        elif execute == 'Расписание':            tt = UML.get_all_date_data()            if tt:                for t in tt:                    key = next(iter(t))                    DATA = t[key]                    dataline = key.replace('-ПН', '')                    l = dataline.split('.')                    dataline += f"-{int(l[0]) + 6}.{l[1]}"                    teext = f'Расписание на неделю {dataline}:\n\n'                    c = 1                    for key, item in DATA.items():                        teext += f"{c}. {key}\n"                        c += 1                    ikb_tt_change = InlineKeyboardMarkup(                        [                            [InlineKeyboardButton("Расписание", callback_data=f'get_v_tt_{key}'),                             InlineKeyboardButton("Изменить", callback_data=f'pass'),                             InlineKeyboardButton("Удалить", callback_data=f'pass'),                             ]                        ]                    )                    msg.reply_text(teext, reply_markup=ikb_tt_change)            else:                msg.reply_text('Расписание не найдено, создайте новое')        elif execute == 'Топ':            msg.reply_text('Coming soon🍀')        elif execute == 'Статистика':            msg.reply_text('Coming soon🍀')        elif execute == 'Зарегистрировать нуждающуюся':            text = 'Шаблон:\n'            text += '[имя] [возраст]\n'            text += '[название или адрес клиники]\n'            text += '[Диагноз]\n'            text += '[сколько сборы] [сколько осталось]\n'            text += '[Дату до которого надо собрать]\n'            msg.reply_text('Хорошо!')            msg.reply_text(text)            switch_turn(msg.from_user.id, 'private_register_needy')        elif execute == 'Создать расписание':            text = "Хорошо! Давай создадим расписание!"            msg.reply_text(text)            lst = GWA()            text = ''            btns = []            for key, el in lst.items():                text += f"{key}.{el} - ...\n"                btns.append([InlineKeyboardButton(text=f"{key}.{el} - nothing\n",                                                  callback_data=f'create_ttd_{msg.from_user.id}_{key}')])                global_cash[msg.from_user.id]['create_tt'] = {'list': lst}                ikb_ctt = InlineKeyboardMarkup(btns)            msg.reply_text(text, reply_markup=ikb_ctt)            switch_off(msg.from_user.id)            return    # endregion    # region REGISTER    if switch['private_check_register'][msg.from_user.id]:        info = msg.text.split('\n')        if len(info) != 5:            msg.reply_text('Мне кажется вы что то забыли, напишите еще раз правильно', reply_markup=kb_delete)            return        fio = info[0]        birthdate = info[1]        email = info[2]        custody_number = info[3]        description = info[4]        txt = ''        if not validFIO(fio):            txt += 'ФИО указано не корректно\n'        if not valid_birthdate(birthdate):            txt += 'Не правильно указан ДР (ДД.ММ.ГГГГ)\n'        if not valid_email(email):            txt += 'Не правильно указан email (example@gmail.com)\n'        if not valid_number(custody_number):            txt += 'Не правильно указан номер\n'        if len(description) <= 3:            txt += f'Описание отсутствует или слишком мало символов\nОписание - <i>{description}</i>'        if txt == '':            global data            data[msg.from_user.id] = {                'id': msg.from_user.id,                'fio': fio,                'birthdate': birthdate,                'email': email,                'custody_number': custody_number,                'description': description,                'dorm': False            }            msg.reply_text('Отлично! Теперь отправь свой номер телефона', reply_markup=kb_get_phone_number)            switch['private_check_phone'][msg.from_user.id] = True            switch['private_check_register'][msg.from_user.id] = True        else:            msg.reply_text(txt, reply_markup=kb_delete)    elif switch['private_change_data'][msg.from_user.id]:        if cmd == 'Изменить':            msg.reply_text('Хорошо, что бы вы хотели изменить?', reply_markup=kb_change_data)        elif cmd == 'Все верно':            msg.reply_text(                'Отлично! ждем подтверждение администраторов. Я обязательно тебе сообщу как только тебя верифицируют, пожалуйста не выключай уведомления в этом чате, тут нет реклам и прочего.',                reply_markup=kb_delete)            user = data[msg.from_user.id]            UM.register(user['birthdate'], msg.from_user.id, user['fio'], user['number'], user['custody_number'],                        user['description'], user['email'], user['dorm'])            getVerifyAdmins(user)            switch['private_change_data'][msg.from_user.id] = False        elif cmd == 'Я из общежития':            data[msg.from_user.id]['dorm'] = True            msg.reply_text('Запомнил, еще что то хотела бы изменить?')        elif switch['private_change_fio'][msg.from_user.id]:            if validFIO(msg.text):                data[msg.from_user.id]['fio'] = msg.text                text = getAnketa(data[msg.from_user.id])                msg.reply_text(text)                msg.reply_text('Хотите еще что нибудь изменить?', reply_markup=kb_change_q)                switch['private_change_fio'][msg.from_user.id] = False            else:                msg.reply_text('Ввод не корректный, введите заново (Иванов Иван Иванович)')        elif switch['private_change_birthdate'][msg.from_user.id]:            if valid_birthdate(msg.text):                data[msg.from_user.id]['birthdate'] = msg.text                text = getAnketa(data[msg.from_user.id])                msg.reply_text(text)                msg.reply_text('Хотите еще что нибудь изменить?', reply_markup=kb_change_q)                switch['private_change_birthdate'][msg.from_user.id] = False            else:                msg.reply_text('Ввод не корректный, введите заново (ДД.ММ.ГГГГ)')        elif switch['private_change_email'][msg.from_user.id]:            if valid_email(msg.text):                data[msg.from_user.id]['email'] = msg.text                text = getAnketa(data[msg.from_user.id])                msg.reply_text(text)                msg.reply_text('Хотите еще что нибудь изменить?', reply_markup=kb_change_q)                switch['private_change_email'][msg.from_user.id] = False            else:                msg.reply_text('Ввод не корректный, введите заново (example@gmail.com)')        elif switch['private_change_custnum'][msg.from_user.id]:            if valid_number(msg.text):                data[msg.from_user.id]['custody_number'] = msg.text                text = getAnketa(data[msg.from_user.id])                msg.reply_text(text)                msg.reply_text('Хотите еще что нибудь изменить?', reply_markup=kb_change_q)                switch['private_change_custnum'][msg.from_user.id] = False            else:                msg.reply_text('Ввод не корректный, введите заново (996 XXX XXX XXX)')        elif switch['private_change_description'][msg.from_user.id]:            data[msg.from_user.id]['description'] = msg.text            text = getAnketa(data[msg.from_user.id])            msg.reply_text(text)            msg.reply_text('Хотите еще что нибудь изменить?', reply_markup=kb_change_q)            switch['private_change_description'][msg.from_user.id] = False        elif cmd == 'ФИО':            msg.reply_text('Напишите новое ФИО (Иванов Иван Иванович)', reply_markup=kb_delete)            switch['private_change_fio'][msg.from_user.id] = True        elif cmd == 'Днюха':            msg.reply_text('Напишите свой ДР (ДД.ММ.ГГГГ)', reply_markup=kb_delete)            switch['private_change_birthdate'][msg.from_user.id] = True        elif cmd == 'Имейл':            msg.reply_text('Напишите свой имейл (example@gmail.com)', reply_markup=kb_delete)            switch['private_change_email'][msg.from_user.id] = True        elif cmd == 'Номер опекуна':            msg.reply_text('Напишите номер', reply_markup=kb_delete)            switch['private_change_custnum'][msg.from_user.id] = True        elif cmd == 'Описание':            msg.reply_text('Напишите новое описание', reply_markup=kb_delete)            switch['private_change_description'][msg.from_user.id] = True    # endregion    # region MAINLOOP    if cmd == 'start':        switch_off(msg.from_user.id)        msg.reply_text(            'Привет, Сактаймер! Спасибо за твое огромное сердце! С твоей помощью мы поможем спасти еще больше людей!',            reply_markup=ReplyKeyboardRemove())        msg.reply_text(            'Здесь ты сможешь записываться на точки раздачи листовок, смотреть свое расписание, оставлять твои отзыва и комментарии, а бот поможет тебе сделать твой волонтерский опыт еще удобнее!')        msg.reply_text(            'Начальные команды:\n\n/admin - Если ты администратор Сактайм\nЭто видят все, не прыгай выше позволенного\n\n/register - Для регистрации волонтеров')    elif cmd == 'register':        switch_off(msg.from_user.id)        if not check_in_db:            text = 'Для начала давай познакомимся\nРасскажи о себе\n\nПример:\n'            text += 'Иванов Иван (ФИ)\n'            text += '01.01.2000\n'            text += 'example@gmail.com (Твой имейл)\n'            text += '+996 XXX XXX XXX (Номер опекуна или родителей)\n'            text += 'Дайте старшего (Описание к твоему профилю)'            msg.reply_text(text, reply_markup=kb_delete)            switch['private_check_register'][msg.from_user.id] = True        elif check_in_db and not verified:            text = 'Хм, вижу твою анкету в базе, повторно проходить регистрацию не обязательно'            msg.reply_text(text, reply_markup=kb_delete)            msg.reply_text(                'Ты в любой момент можешь взглянуть на свой профиль командой "<code>Профиль</code>"\nИли же - /profile')        else:            text = 'Хм, вижу ты уже в системе\nОтлично! Тебе доступен бот! в регистрации нет необходимости'            msg.reply_text(text, reply_markup=kb_delete)            msg.reply_text(                'Ты в любой момент можешь взглянуть на свой профиль командой "<code>Профиль</code>"\nИли же - /profile')    elif cmd in ['profile', 'Профиль'] or execute == 'Профиль':        switch_off(msg.from_user.id)        if check_in_db and verified:            get_user = UM.get_user_data(msg.from_user.id)            text = getAnketa(get_user[0])            msg.reply_text(text)        else:            msg.reply_text('Вы не прошли регистрацию, пожалуйста пройдите\n\nКоманда - /register')    elif cmd in ['menu', 'Меню']:        switch_off(msg.from_user.id)        if user.id in admins:            msg.reply_text('Установка меню ⚙', reply_markup=getVMKBadmin())        else:            if verified:                tt = UML.get_all_date_data()                upd = []                for time in tt:                    t = next(iter(time))                    date, _ = t.split('-')                    upd.append(date)                upt = get_closest_dates_str(upd)                msg.reply_text('Установка меню ⚙', reply_markup=getVMKB(upt))    elif cmd in ['адм запрос', 'admin']:        switch_off(msg.from_user.id)        msg.reply_text('Хорошо! твой запрос на повышение до администратора отправлен')        getNewAdmin(msg.from_user.id)    elif not check_in_db and not verified:        msg.reply_text('Вы не прошли регистрацию, пожалуйста пройдите\n\nКоманда - /register')        pass    elif execute == 'Мое расписание':        switch_off(msg.from_user.id)        if check_in_db and verified:            msg.reply_text('Coming soon🍀')        else:            msg.reply_text('Вы не прошли регистрацию, пожалуйста пройдите\n\nКоманда - /register')    elif execute == 'Информация о текущих нуждающихся':        switch_off(msg.from_user.id)        if check_in_db and verified:            needies = UMneedy.get_all_needy_data()            if needies:                txt = 'Список нуждающихся:'                btns = []                for needy in needies:                    btns.append([InlineKeyboardButton(text=f"{needy['name']} - {needy['age']} лет",                                                      callback_data=f"v_needy_{needy['name']}")])                reply_markup = InlineKeyboardMarkup([btns])                msg.reply_text(txt, reply_markup=reply_markup)            else:                msg.reply_text('В базе данных нет информации о нуждающихся')        else:            msg.reply_text('Вы не прошли регистрацию, пожалуйста пройдите\n\nКоманда - /register')    elif execute.startswith('Записаться на эту неделю') and check_in_db and verified:        switch_off(msg.from_user.id)        DATA = UML.get_all_date_data()        users = UM.get_all_users_data()        data_list_indexes = []        for d in DATA:            data_list_indexes.append(next(iter(d)))        this = True        S = UML.get_date(get_max_date(data_list_indexes, m=this))        SATA = S[next(iter(S))]        text = "Расписание на эту неделю:\n\n"        btns = []        for key, item in SATA.items():            c = 1            allc = 0            for key1, item1 in item.items():                for itemn in item1:                    if isinstance(itemn, dict):                        item3 = itemn                    elif isinstance(itemn, str):                        checkpoint = itemn                for index, item4 in item3.items():                    if index.isdigit():                        count = item4['count'] - len(item4['volunteers'])                        allc += count                    else:                        if isinstance(item4, dict):                            count = item3['count'] - len(item3['volunteers'])                            allc += count                c += 1            if allc != 0:                count_txt = f"{allc} свободных мест ✅"            elif allc == 0:                count_txt = f"{allc} свободных мест ❌"            else:                count_txt = f"{allc} свободных мест ✔"            text += f"<b>{key}</b> : <i>{count_txt}</i>\n"            text += '\n'            btns.append([InlineKeyboardButton(                text=f"{key}",                callback_data=f"j_tt_{next(iter(S))}_{key}_{msg.from_user.id}_{this}"            )])        reply_markup = InlineKeyboardMarkup(btns)        msg.reply_text(text, reply_markup=reply_markup, disable_web_page_preview=True)    elif execute.startswith('Записаться на следующую неделю') and check_in_db and verified:        switch_off(msg.from_user.id)        DATA = UML.get_all_date_data()        users = UM.get_all_users_data()        data_list_indexes = []        for d in DATA:            data_list_indexes.append(next(iter(d)))        S = UML.get_date(get_max_date(data_list_indexes))        SATA = S[next(iter(S))]        text = "Расписание на следующую неделю:\n\n"        btns = []        for key, item in SATA.items():            c = 1            allc = 0            for key1, item1 in item.items():                for itemn in item1:                    if isinstance(itemn, dict):                        item3 = itemn                    elif isinstance(itemn, str):                        checkpoint = itemn                for index, item4 in item3.items():                    if index.isdigit():                        count = item4['count'] - len(item4['volunteers'])                        allc += count                    else:                        if isinstance(item4, dict):                            count = item3['count'] - len(item3['volunteers'])                            allc += count                c += 1            if allc != 0:                count_txt = f"{allc} свободных мест ✅"            elif allc == 0:                count_txt = f"{allc} свободных мест ❌"            else:                count_txt = f"{allc} свободных мест ✔"            text += f"<b>{key}</b> : <i>{count_txt}</i>\n"            text += '\n'            btns.append([InlineKeyboardButton(                text=f"{key}",                callback_data=f"j_tt_{next(iter(S))}_{key}_{msg.from_user.id}"            )])        reply_markup = InlineKeyboardMarkup(btns)        msg.reply_text(text, reply_markup=reply_markup, disable_web_page_preview=True)    elif execute == 'Предложка и обратная связь' and check_in_db and verified:        switch_off(msg.from_user.id)        msg.reply_text(execute, reply_markup=kb_feedback)    elif execute == 'О Нас' and check_in_db and verified:        switch_off(msg.from_user.id)        text = get_info()        msg.reply_text(text)    elif execute == 'Связаться' and check_in_db and verified:        switch_off(msg.from_user.id)        for adm in admins:            user = app.get_users(adm)            username = user.first_name            if user.last_name:                username += f' {user.last_name}'            msg.reply_text(f"<u>{getUserLink(user.id, username)}</u>")    elif execute == 'Отзыв' and check_in_db and verified:        switch_off(msg.from_user.id)        msg.reply_text('Хорошо! Напишите отзыв о нашем боте🤍')        switch_turn(msg.from_user.id, 'private_get_feedback')    elif execute == 'Предложка (идеи, мероприятия, конкурсы)' and check_in_db and verified:        switch_off(msg.from_user.id)        msg.reply_text('Хорошо! Напишите вашу идею🤍\nНам важно услышать вас, так как это поможет нам двигаться дальше')        switch_turn(msg.from_user.id, 'private_get_idea')    elif switch['private_get_feedback'][msg.from_user.id]:        userok = UM.get_user_data(msg.from_user.id)[0]        text = f"Отзыв от {getUserLink(userok['id'], userok['fio'])}:\n\n"        text += f"{msg.text}"        console(text)        tt = UML.get_all_date_data()        upd = []        for time in tt:            t = next(iter(time))            date, _ = t.split('-')            upd.append(date)        upt = get_closest_dates_str(upd)        msg.reply_text('Сообщение отправлено администраторам!', reply_markup=getVMKB(upt))    elif switch['private_get_idea'][msg.from_user.id]:        userok = UM.get_user_data(msg.from_user.id)[0]        text = f"Предложение от {getUserLink(userok['id'], userok['fio'])}:\n"        text += f"Примите к сведению\n\n"        text += f"{msg.text}"        console(text)        tt = UML.get_all_date_data()        upd = []        for time in tt:            t = next(iter(time))            date, _ = t.split('-')            upd.append(date)        upt = get_closest_dates_str(upd)        msg.reply_text('Сообщение отправлено администраторам!', reply_markup=getVMKB(upt))    # endregion# endregion# region CALLBACKcb_cash = {}rg_cash = {}@app.on_callback_query()def button(bot, update):    log(f"{update.data} --- {len(update.data.encode('utf-8'))}")    data = update.data.split("_")    global global_cash    global admins    global cb_cash    global rg_cash    if data[0] == "admchat":        if data[1] == 'request':            if data[2] == 'succes':                if update.from_user.id in admins:                    check_in_db = UM.check_in_db(int(data[3]))                    if check_in_db:                        UM.update_user(int(data[3]), {'verified': True})                        update.message.edit_text(update.message.text + '\nПодтвержден✅')                        tt = UML.get_all_date_data()                        upd = []                        for time in tt:                            t = next(iter(time))                            date, _ = t.split('-')                            upd.append(date)                        upt = get_closest_dates(upd)                        log(upt)                        app.send_message(int(data[3]),                                         '✅Поздравляю, твой запрос только что подтвердили, тебе доступно меню',                                         reply_markup=getVMKB(upt))                    else:                        pass                else:                    update.answer('Любопытной варваре, на базаре нос оторвали!')            elif data[2] == 'denied':                if update.from_user.id in admins:                    check_in_db = UM.check_in_db(int(data[3]))                    if not check_in_db:                        UM.update_user(int(data[3]), {'verified': False})                        update.message.edit_text(update.message.text + '\nОтклонен❌')                        app.send_message(int(data[3]), 'Плохая новость, твой запрос отклонен(❌')                    else:                        app.delete_messages(update.chat.id, update.message.id)                else:                    update.answer('Любопытной варваре, на базаре нос оторвали!')        elif data[1] == 'newadm':            if data[2] == 'succes':                if update.from_user.id in admins:                    UM.delete_user(int(data[3]))                    user = app.get_users(int(data[3]))                    username = user.first_name                    if user.last_name:                        username += user.last_name                    update_config(cfg, 'ADMIN_LIST', username, data[3])                    admins.append(int(data[3]))                    update.message.edit_text(update.message.text + '\nПодтвержден✅')                    app.send_message(int(data[3]),                                     '✅Поздравляю, твой запрос только что подтвердили, тебе доступно меню',                                     reply_markup=getVMKBadmin())                else:                    update.answer('Любопытной варваре, на базаре нос оторвали!')            elif data[2] == 'denied':                if update.from_user.id in admins:                    UM.update_user(int(data[3]), {'verified': False})                    update.message.edit_text(update.message.text + '\nОтклонен❌')                    app.send_message(int(data[3]), 'Плохая новость, твой запрос отклонен(❌')                else:                    update.answer('Любопытной варваре, на базаре нос оторвали!')    elif data[0] == 'change':        if data[1] == 'data':            if data[3] == 'JUMA':                pass            elif data[3] == 'DEFAULT':                pass    elif data[0] == 'get':        if data[1] == 'tt':            if data[2] == 'for':                datacash = global_cash[int(data[4])]['dataset_all'][data[3]]                txt = ''                c = 1                btns = []                for key, item in datacash.items():                    txt += f"{c}. {key}\n"                    button = InlineKeyboardButton(text=f"{c}. {key}",                                                  callback_data=f"get_tt_od_{data[4]}_{data[3]}")                    btns.append(button)                    c += 1                if btns:                    button = InlineKeyboardButton(text="Назад",                                                  callback_data=f"get_tt_fd_{data[4]}")                    btns.append(button)                    reply_markup = InlineKeyboardMarkup([btns])                    update.message.edit_text(txt, reply_markup=reply_markup)                else:                    update.message.edit_text('Извините произошла ошибка')            elif data[2] == 'od':                datacash = global_cash[int(data[3])]['dataset_all'][data[4]]                checkpoint = datacash[1]                DATA = datacash[0]                text = f"регион - {data[5]}\n"                text += f"{checkpoint}\n"                for key, item in DATA.items():                    offset = 5 - len(str(key))                    text += f"{key}.{' ' * offset}{item['name']}\n"                    text += f"    ссылка - {item['link']}\n"                    text += f"    кому сбор - {item['person']}\n\n"                reply_markup = InlineKeyboardMarkup([                    [InlineKeyboardButton(text='Назад',                                          callback_data=f"get_tt_for_{data[4]}_{data[3]}"                                          )]                ])                update.message.edit_text(text, reply_markup=reply_markup)            elif data[2] == 'fd':                all_tt = global_cash[int(data[3])]['dataset_all']                btns = []                txt = ''                c = 1                for key, item in all_tt.items():                    txt += f"{c}. {key.replace('-', ' ')}\n"                    button = InlineKeyboardButton(text=f"{c}. {key.replace('-', ' ')}",                                                  callback_data=f"get_tt_for_{key}_{data[3]}")                    btns.append([button])                    c += 1                button = InlineKeyboardButton(text="Сохранить",                                              callback_data=f"save_tt_{data[3]}")                btns.append([button])                text = txt                txt += f"\nНажмите сохранить, чтобы расписание отправилось в базу данных"                reply_markup = InlineKeyboardMarkup(btns)                update.message.edit_text(txt, reply_markup=reply_markup)    elif data[0] == 'create':        if data[1] == 'ttd':            user_id = int(data[2])            key = int(data[3])            K = global_cash[user_id]['create_tt']['list'][key]            text = f"{K}:ㅤㅤㅤㅤㅤㅤㅤㅤ\n...\n"            global_cash[user_id]['create_tt']['text'] = text            btns = [                [InlineKeyboardButton(text='Добавить район', callback_data=f'add_r_{user_id}_{key}')],                [InlineKeyboardButton(text='Добавить локацию', callback_data=f'add_l_{user_id}_{key}')],                [InlineKeyboardButton(text='Добавить мечеть', callback_data=f'add_m_{user_id}_{key}')],                [InlineKeyboardButton(text='Добавить другое', callback_data=f'add_o_{user_id}_{key}')],                [InlineKeyboardButton(text='Назад', callback_data=f'create_rt_{user_id}_{key}'),                InlineKeyboardButton(text='Пустой день', callback_data=f'add_n_{user_id}_{key}')],            ]            keyboard_markup = InlineKeyboardMarkup(btns)            update.message.edit_text(text, reply_markup=keyboard_markup)        elif data[1] == 'rt':            user_id = int(data[2])            archive = global_cash[user_id]['create_tt']['list']            text = ''            btns = []            for key, el in archive.items():                text += f"{key}.{el} - ...\n"                btns.append([InlineKeyboardButton(text=f"{key}.{el} - nothing\n",                                                  callback_data=f'create_ttd_{user_id}_{key}')])                global_cash[user_id]['create_tt'] = {'list': archive}            ikb_ctt = InlineKeyboardMarkup(btns)            update.message.edit_text(text, reply_markup=ikb_ctt)            switch_off(user_id)    elif data[0] == 'add':        if data[1] == 'r':            user_id = int(data[2])            key = int(data[3])            K = global_cash[user_id]['create_tt']['list'][key]            text = global_cash[user_id]['create_tt']['text']        elif data[1] == 'l':            user_id = int(data[2])            key = int(data[3])            K = global_cash[user_id]['create_tt']['list'][key]            text = global_cash[user_id]['create_tt']['text']        elif data[1] == 'm':            user_id = int(data[2])            key = int(data[3])            K = global_cash[user_id]['create_tt']['list'][key]            text = global_cash[user_id]['create_tt']['text']        elif data[1] == 'o':            user_id = int(data[2])            key = int(data[3])            K = global_cash[user_id]['create_tt']['list'][key]            text = global_cash[user_id]['create_tt']['text']        elif data[1] == 'n':            user_id = int(data[2])            key = int(data[3])            K = global_cash[user_id]['create_tt']['list'][key]            text = global_cash[user_id]['create_tt']['text']# endregion# region CYCLESdef FRIDAY():    def job():        datelines = UML.get_all_date_data()    job()    # friday = BackgroundScheduler()    # friday.add_job(job, trigger='cron', day_of_week='fri', hour=13, minute=0)# endregion"""└│├"""log('WITHOUT ERRORS', 'DEBUG')#FRIDAY()if switch_starter:    start_upd(app_starter)app.run()